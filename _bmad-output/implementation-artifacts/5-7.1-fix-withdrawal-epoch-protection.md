# Story 5-7.1: Fix LP Withdrawal Epoch Protection

## Story

As a liquidity provider, I want my withdrawal funds to be protected from being used in active epochs, so that the pool remains solvent and my withdrawal is guaranteed after cooldown.

## Status: in-progress

## Context

**Vulnerability:** The on-chain `process_withdrawal` instruction had no check preventing LP withdrawals while an epoch is active. An LP could drain pool reserves mid-epoch, leaving the pool insolvent for paying winners.

**Solution:** Added a `pending_withdrawal_shares` field to the Pool struct that tracks the aggregate LP shares pending withdrawal. At settlement rebalancing, the USDC value is computed dynamically from current share prices and subtracted from total reserves before the 50:50 split — effectively "reserving" the withdrawal funds. `process_withdrawal` is blocked during active epochs. A permissionless `crank_process_withdrawal` instruction enables the crank bot to auto-process pending withdrawals in the gap between settlement and next epoch creation.

## Acceptance Criteria

1. Given an active epoch, When I try to complete a withdrawal, Then the transaction fails with `WithdrawalBlockedDuringEpoch`
2. Given a pending withdrawal request, When the epoch settles and rebalances, Then the withdrawal amount is reserved (excluded from the 50:50 rebalance for the next epoch)
3. Given a pending withdrawal with elapsed cooldown and no active epoch, When the crank bot runs, Then it auto-processes the withdrawal before creating the next epoch
4. Given a pending withdrawal during an active epoch, When I view the LP dashboard, Then I see "Waiting for epoch settlement..." instead of the Complete button
5. Given the pool has `pending_withdrawal_shares > 0`, When `request_withdrawal` is called, Then `pending_withdrawal_shares` is incremented by the requested shares amount
6. Given `process_withdrawal` or `crank_process_withdrawal` completes, Then `pending_withdrawal_shares` is decremented by the processed shares

## Tasks

- [x] Task 1: Add `pending_withdrawal_shares` to Pool struct (AC: #2, #5, #6)
  - [x] 1.1: Add field to `anchor/programs/fogopulse/src/state/pool.rs`
  - [x] 1.2: Initialize to 0 in `anchor/programs/fogopulse/src/instructions/create_pool.rs`
- [x] Task 2: Add `WithdrawalBlockedDuringEpoch` error (AC: #1)
  - [x] 2.1: Add error variant to `anchor/programs/fogopulse/src/errors.rs`
- [x] Task 3: Update `request_withdrawal` to increment pending shares (AC: #5)
  - [x] 3.1: Make pool mutable in accounts struct
  - [x] 3.2: Increment `pool.pending_withdrawal_shares` by `shares_amount`
- [x] Task 4: Update `process_withdrawal` with epoch guard + decrement (AC: #1, #6)
  - [x] 4.1: Add `require!(pool.active_epoch.is_none())` check
  - [x] 4.2: Decrement `pool.pending_withdrawal_shares` by `pending_shares` after transfer
- [x] Task 5: Update `settle_epoch` rebalance to reserve withdrawal funds (AC: #2)
  - [x] 5.1: Compute reserved USDC dynamically from `pending_withdrawal_shares * total_reserves / total_lp_shares`
  - [x] 5.2: Subtract reserved amount from total reserves before 50:50 split
- [x] Task 6: Build, deploy, and reinitialize (AC: all)
  - [x] 6.1: `anchor build` in WSL
  - [x] 6.2: `anchor deploy` to testnet (TX: 5rkhRcvHHckgpg2Zx8Ng1J8NVGHarvxxvcsmJJzY1HbU62M3i8vAvL4z4w19M4JKZHzd7ZRVrwRWadh6Xbeya42S)
  - [x] 6.3: Regenerate IDL → copy to `web/src/lib/fogopulse.json`
  - [x] 6.4: Reinitialize pools (all 4 pools recreated at 121 bytes, seeded 1000 USDC each)
- [x] Task 7: Update crank bot to process withdrawals between settle and create (AC: #3)
  - [x] 7.1: Add `findPendingWithdrawals(poolPda)` helper with cooldown filtering
  - [x] 7.2: Add `buildAndSendProcessWithdrawalTx(...)` using `crank_process_withdrawal`
  - [x] 7.3: Insert withdrawal processing loop in settle chain (after settle, before create_epoch)
  - [x] 7.4: Handle `NoPendingWithdrawal`, `CooldownNotElapsed`, `WithdrawalBlockedDuringEpoch` errors gracefully
- [x] Task 8: Update frontend error handling (AC: #4)
  - [x] 8.1: Add `WithdrawalBlockedDuringEpoch` to `web/src/lib/transaction-errors.ts`
  - [x] 8.2: Add to `nonRecoverable` array
- [x] Task 9: Update `LpPendingWithdrawal` component for epoch-active state (AC: #4)
  - [x] 9.1: Add `isEpochActive` detection from `pool.activeEpoch`
  - [x] 9.2: Show "Waiting for epoch settlement..." when cooldown elapsed + epoch active
  - [x] 9.3: Disable Complete button during active epoch
- [x] Task 10: Create permissionless `crank_process_withdrawal` instruction (AC: #3) [Code Review Fix]
  - [x] 10.1: Create `anchor/programs/fogopulse/src/instructions/crank_process_withdrawal.rs`
  - [x] 10.2: Register in `mod.rs` and `lib.rs`

- [x] Task 11: Fix stale byte offsets in all raw Pool parsers (post-deploy fix)
  - [x] 11.1: Update `web/src/lib/transactions/create-epoch.ts` — offset 64 → 72 for `fetchPoolNextEpochId`
  - [x] 11.2: Update `anchor/scripts/create-test-epoch.ts` — add `pending_withdrawal_shares` skip (offset += 8)
  - [x] 11.3: Update `anchor/scripts/force-close-epoch.ts` — add skip
  - [x] 11.4: Update `anchor/scripts/settle-epoch.ts` — add skip
  - [x] 11.5: Update `anchor/scripts/advance-epoch.ts` — add skip
  - [x] 11.6: Update `anchor/scripts/check-pool-liquidity.ts` — add skip
  - [x] 11.7: Update `anchor/scripts/verify-protocol.ts` — add skip
  - Note: `claim-refund.ts` only reads `assetMint` (before shifted fields), not affected. `claim-payout.ts` doesn't parse pool bytes. `crank-bot/crank-bot.ts` was already fixed.
- [x] Task 12: Add `admin_close_epoch` instruction for orphaned epoch cleanup
  - [x] 12.1: Create `anchor/programs/fogopulse/src/instructions/admin_close_epoch.rs` (follows `admin_close_pool.rs` pattern)
  - [x] 12.2: Register in `mod.rs` and `lib.rs`
  - [x] 12.3: Build and deploy to testnet
  - [x] 12.4: Create `anchor/scripts/close-orphaned-epochs.ts` cleanup script
  - [x] 12.5: Run cleanup to close orphaned epoch accounts (use `--max N` for pools with many old epochs)
  - Context: After pool reinitialization, `next_epoch_id` resets to 0 but old epoch PDAs still exist on-chain (same pool PDA → same epoch PDA derivation). `create_epoch` uses `init` which fails on existing accounts with "Allocate: account already in use".

## Dev Notes

- Pool account size increased by 8 bytes (new `pending_withdrawal_shares: u64` field). Existing pools on testnet need reinitialization.
- **All raw byte parsers** across scripts and frontend must account for the new field. The `pending_withdrawal_shares` field sits between `total_lp_shares` and `next_epoch_id`, shifting `next_epoch_id` from offset 64 to offset 72. Crank bot was fixed during initial implementation; scripts and frontend were fixed in Task 11.
- After pool reinitialization, orphaned epoch accounts block new epoch creation because the pool's `next_epoch_id` resets to 0 but the old epoch 0 PDA still exists. The `admin_close_epoch` instruction (Task 12) resolves this by closing orphaned epoch accounts.
- `pending_withdrawal_shares` tracks shares (not USDC) to avoid drift between request and processing time. USDC value is computed dynamically at settlement.
- `settle_epoch` computes reserved USDC as `pending_withdrawal_shares * total_reserves / total_lp_shares` before the 50:50 rebalance.
- `crank_process_withdrawal` is a permissionless variant of `process_withdrawal` — enables the crank bot to auto-process withdrawals without needing user session delegation.
- Crank bot's `findPendingWithdrawals` filters by cooldown elapsed to avoid wasting transactions.
