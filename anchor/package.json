{
  "name": "anchor",
  "version": "0.0.0",
  "private": true,
  "description": "NOTE: anchor build/test commands must run in WSL where Anchor CLI is installed",
  "scripts": {
    "build": "echo 'Run anchor build in WSL (not Windows)'",
    "build:wsl": "anchor build",
    "test": "echo 'Run anchor test in WSL (not Windows)'",
    "test:wsl": "anchor test"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/spl-token": "^0.4.6",
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "@types/bn.js": "^5.2.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.10",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0"
  }
}
